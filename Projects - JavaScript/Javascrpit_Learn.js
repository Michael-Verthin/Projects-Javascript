/*
Why study JavaScript?
    JavaScript is one of the 3 languages all web developers must learn:
        1. HTML - to define the content of web pages
        2. CSS - to scecify the layout of web pages
        3. JavaScript - to program the behavior of web pages

< Introduction >
    JavaScript can change HTML content:
        document.getElementById("example").innerHTML = "Hello JavaScript"; 
        // that example "finds" an HTML element and changes it content (innerHTML) to 'Hello JavaScript'
    JavaScript can change HTML style:
        document.getElementById("example").style.fontSize = 35px;
    Java can hide/show HTML element:
        document.getElementById("example").style.display = "none" // hide
        document.getElementById("example").style.display = "block" // show
< Where To >
    the <script> tag:
        in HTML, javaScript is inserted between <script> and </script> tags
        the <script> can be placed in the <head> and in the <body>
        you can place any number of <script> in a document
    external JavaScript:
        JavaScript ahve the .js extenstion
        <script src="myScript.js"></script>
< Output >
    JavaScript display possibilities:
        writing into an HTML elemnt, using - innerHTML
        writing into the HTML outpost, using - document.write()
        writing into an alert box, using - window.alert()
        writing into the browser console, using - console.log()
< Statements >
    a computer program is a list of "instructions" to be "executed" by the computer
    in programming language, these programming instructions are called - statements
    a JavaScript program is a list of programming statements.
    JavaScript statements are composed of:
        values, operators, expressions, keywords and comments
    semicolons;
        semicolons separate JavaScript statements
            var a, b, c;  // declare 3 variables
            a = 5;        // assign the value 5 to a
            b = 6;        // assign the value 6 to b
            c = a + b;    // assign the sum of a and b to c
    when separated by semicolons, multiple statements on one line are allowed:
        a = 5; b = 6; c = a + b;
    code block:
        JavaScript statements are grouped together in coode blocks,inside curly brackets {...}
< Syntex >
    Syntex are a set of rules, how JavaScript programs are constracted
        var x, y, z;   // declare variables
        x = 5; y = 6;  // assign values
        z = y + x;     // compute values
    The JavaScript syntax defines two types of values:
        1. fixed values
        2. variable values
    Fixed values are called Literals
    Variable values are called Variables
    

*/

function multiplay(arr, num){
    var sum
    
}
arrey=[]
multiplay(arrey,1)